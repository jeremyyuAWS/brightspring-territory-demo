# Copy this file to .env.local and fill in your values (.env.local is gitignored).

# Public Mapbox access token (starts with "pk."), restricted to your demo URLs.
# Without it, the app runs fully with the built-in inline-SVG map fallback.
VITE_MAPBOX_ACCESS_TOKEN=
